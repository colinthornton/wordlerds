{
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "db": "bun run --bun kysely"
  },
  "dependencies": {
    "hono": "^4.12.8",
    "kysely": "^0.28.12",
    "kysely-bun-sqlite": "^0.4.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10",
    "kysely-ctl": "^0.20.0"
  }
}
